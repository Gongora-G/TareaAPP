# === Configuración de la Base de Datos (PostgreSQL) ===
PGHOST=localhost            # Host del servidor PostgreSQL (en desarrollo local: localhost)
PGUSER=postgres             # Usuario de la base de datos (en desarrollo local: postgres)
PGPASSWORD=mi_contraseña    # Contraseña del usuario de la base de datos
PGDATABASE=crud_nodes_db    # Nombre de la base de datos
PGPORT=5432                 # Puerto de PostgreSQL (por defecto es 5432)

# En Render, estas variables deben configurarse en el panel de variables de entorno,
# no necesitas este archivo .env en producción.

# === Configuración de la Sesión (Express-session) ===
SESSION_SECRET=mi_clave_secreta  # Clave secreta para firmar las sesiones

# NOTA:
# 1. Este archivo es para uso exclusivo en desarrollo local.
# 2. Nunca subas este archivo al repositorio. Añade .env a tu .gitignore.
